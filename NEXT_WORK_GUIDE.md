# JP Travel Bali — 다음 작업 가이드

## 현재까지 완료된 작업 (v8 → v9)

### v8 작업 (이전)
- 한국어 조사 자동 처리 (을/를, 은/는, 이/가 등) → 0개 미처리
- 도입부 20개+ 패턴 다양화 → 924개 전부 고유
- 반복 구문 제거 (10년차 블로거, 숨은 명소 등)
- 이미지 ALT 전면 재작성 → 7678개 고유
- robots.txt + sitemap.xml 생성

### v9 작업 (현재)
- build_v9.py 신규 빌드 스크립트 작성
- 924개 HTML 전면 재생성 (0 오류)
- 카테고리별 H2 구조 14개 변형 적용
- 경험 중심 도입부 15개 패턴
- 고유 Q&A (카테고리별 4개 x 14페이지)
- 장소별 상세 설명 (카테고리별 5개 패턴)
- 추가 섹션 (실패 팁, 시간대별 팁, 우천 대안, 추천/비추천)
- 여행 에피소드 6개 패턴
- 가격 비교표 (로컬 vs 투어 vs 호텔)
- 예산 절약 팁
- 관련 지역 내부 링크
- MRT 제휴 링크 정상 배치 (924/924)
- SEO 롱테일 제목 (924개 고유)
- 메타 설명 고유화
- Schema.org Article JSON-LD
- OG/Twitter 메타태그
- 다크모드 대응 CSS
- 읽기 진행률 바
- 반응형 모바일 최적화

---

## 현재 상태 (검증 결과)

| 항목 | 현재 | 목표 |
|------|------|------|
| HTML 수 | 924개 | 924개 |
| 제목 중복 | 0개 | 0개 |
| 메타 설명 중복 | 330개 | 0개 |
| 평균 본문 길이 | 936자 | 2500자+ |
| 1500자 미만 | 900개 | 0개 |
| 한자 포함 | 0개 | 0개 |
| 이모지 포함 | 924개 | 0개 |
| 조사 오류 | 0개 | 0개 |
| 같은 페이지 이미지 중복 | 0개 | 0개 |
| 이미지 누락 | 1개 | 0개 |
| MRT 링크 | 924/924 | 924/924 |
| robots.txt | 있음 | 있음 |
| sitemap.xml | 있음 | 있음 |

---

## 다음에 해야 할 작업 (우선순위순)

### 1. 본문 길이 확장 (긴급)
- 현재 평균 936자 → 목표 2500자+
- 장소 설명, 팁, 비교 분석 등을 대폭 확장 필요
- 각 섹션에 300~500자씩 추가해야 목표 달성 가능
- build_v9.py의 `generate_places_html`, `generate_extra_sections`, `generate_budget_tips_html` 함수 확장

### 2. 이모지 제거 (긴급)
- 924페이지 전부 이모지 포함
- H2 제목에서 이모지 문자 제거 필요
- build_v9.py의 H2_STRUCTURES에서 이모지 제거

### 3. 메타 설명 중복 제거 (중요)
- 330개 중복 → 0개 목표
- 지역+카테고리+페이지별 고유 메타 설명 생성 로직 강화
- generate_meta_desc 함수의 템플릿 확장 (현재 5개 → 20개+)

### 4. 이미지 누락 수정 (중요)
- 1개 이미지 누락 → 확인 및 수정
- 이미지 매핑 파일 점검

### 5. 반복 구문 추가 제거 (중요)
- "숨은 명소" 198회 → 다양한 표현으로 교체
- "추천이에요" 341회 → 다양한 표현으로 교체
- "가격 비교" 484회 → 다양한 표현으로 교체
- "실전 팁" 330회 → 다양한 표현으로 교체
- "2026년 기준" 924회 → 다양한 표현으로 교체

### 6. ALT 텍스트 추가 고유화 (선택)
- 현재 7678개 고유이나 지역 내 반복 존재
- 이미지별 구체적 장면 묘사 추가

### 7. 이미지 재사용 최소화 (선택)
- 3+ 페이지 재사용 이미지 763개
- 이미지 소스 다양화 필요 (Openverse, Flickr, Wikimedia)

### 8. 최종 검증 리포트 생성 (필수)
- BALI_QUALITY_AUDIT_AFTER.txt/json 생성
- 모든 항목 통과 확인
- BALI_NEXT_WORK_GUIDE.md 업데이트

---

## 확장 가이드 (다른 국가로 확장 시)

### 구조
```
output/html/{국가}/
output/images/{국가}/
build_{국가}.py
image_mapping_{국가}.json
```

### 데이터 준비
1. `{국가}_DATA` 딕셔너리 정의 (지역별 상세 정보)
2. 이미지 매핑 파일 생성 (스크래핑 또는 수동)
3. 카테고리 정의 (food, culture, beach, nature, shopping, transport)

### 빌드
1. `build_{국가}.py` 실행
2. 검증 스크립트 실행
3. robots.txt, sitemap.xml 생성
4. git commit & push

### 주의사항
- 한국어 조사 처리 필수
- 이모지/한자 제거
- 반복 문구 제거
- 고유 제목/메타 설명
- MRT 제휴 링크 배치
- 이미지 중복 방지
